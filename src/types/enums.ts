export enum SortTypes {
  None = "none",
  Price = "price",
  Author = "author",
  Date = "date",
}

export enum SortOrder {
  Asc = "Asc",
  Desc = "Desc",
}

export enum Urls {
  Books = "http://localhost:3000/books",
}
